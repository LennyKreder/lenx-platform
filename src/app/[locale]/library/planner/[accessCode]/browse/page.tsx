import { BrowseContainer } from '@/components/library/browse/BrowseContainer';

export const metadata = {
  title: 'Browse Planners',
  description: 'Browse and download all available planner files',
};

export default function BrowsePage() {
  return <BrowseContainer />;
}
