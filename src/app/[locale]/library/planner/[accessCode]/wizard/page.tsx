import { WizardContainer } from '@/components/library/wizard/WizardContainer';

export const metadata = {
  title: 'Find Your Planner',
  description: 'Step-by-step wizard to find your perfect planner',
};

export default function WizardPage() {
  return <WizardContainer />;
}
